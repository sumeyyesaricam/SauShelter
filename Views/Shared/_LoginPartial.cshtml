@if (@Session["user_id"] == null || @Session["user_id"] == ("00000000-0000-0000-0000-000000000000"))
{
    @Html.ActionLink("Üye Ol", "Register", "Insiders")
    @: |
    @Html.ActionLink("Giriş Yap", "Login", "Insiders")
}
else
{
    @: Hoşegeldin <b></b>!

    @: |
    @Html.ActionLink("Çıkış Yap", "LogOff", "Insiders")
}
